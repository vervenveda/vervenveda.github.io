# CNAME Decision Required

The repository README identifies `www.vervenveda.com` as the intended GitHub Pages custom domain.

Do not add a root file named `CNAME` until you confirm that `www.vervenveda.com` should display the Verve N Veda public gateway rather than another current site.

Once confirmed, create a file named exactly `CNAME` containing only:

```text
www.vervenveda.com
```
